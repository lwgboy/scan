<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xxtt.scan"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WRITE_PROFILE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/uhf_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"></activity>
        <activity
            android:name=".SettingActivity"
            android:launchMode="singleTask"></activity>
        <activity
            android:name=".MoreHandleActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".SettingPower"
            android:launchMode="singleTask" />
        <activity
            android:name=".DistActivity"
            android:label="@string/title_activity_dist"></activity>
        <activity
            android:name=".SetNumberActivity"
            android:label="@string/title_activity_set_nubmer"></activity>
    </application>

</manifest>